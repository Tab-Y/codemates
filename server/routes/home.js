const router = require('express').Router();
const {  } = require('../controllers') // find specific functions file needed for this part of routes

// each CRUD route
// POST
// GET
// PUT
// DELETE

// router.route('/').post(function for response data)

module.exports = router;